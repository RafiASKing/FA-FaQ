# === GOOGLE GEMINI ===
GOOGLE_API_KEY="your-google-api-key-here"

# === CHROMADB ===
# Server mode recommended (even for local dev) — embedded mode is unstable
# For local dev: use localhost (run `docker compose up chroma-server -d`)
# For Docker: use chroma-server (container network)
CHROMA_HOST=localhost
CHROMA_PORT=8000

# === ADMIN ===
ADMIN_PASSWORD_HASH='$2b$12$your-bcrypt-hash-here'

# === WHATSAPP BOT (optional, only for bot mode) ===
WA_BASE_URL=http://wppconnect:21465
WA_SESSION_KEY=THISISMYSECURETOKEN
BOT_IDENTITIES=6281234567890
BOT_MIN_SCORE=80.0
BOT_MIN_GAP=10.0
